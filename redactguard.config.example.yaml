# RedactGuard Policy — ejemplo para CLI
# Usar: redactguard sanitize -c redactguard.config.yaml input.png output.png

policy:
  name: default
  fail_on_pii: false

zones:
  - x: 10
    y: 20
    width: 100
    height: 40
  # - x: 50
  #   y: 60
  #   width: 80
  #   height: 30
